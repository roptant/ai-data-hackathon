{
  "name": "local-dictation-ui",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "TypeScript UI compiled with tsc only; no bundler or runtime dependencies. The Tauri API is used through the injected global.",
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../scripts/copy-ui-assets.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
