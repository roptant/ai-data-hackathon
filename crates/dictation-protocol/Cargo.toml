[package]
name = "dictation-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Wire types shared by the Local Dictation upload client and training server"

[dependencies]
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
