{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability for the migration status window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

