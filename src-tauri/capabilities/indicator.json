{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "indicator-window",
  "description": "The recording overlay can read status and stop or cancel; nothing else.",
  "windows": ["indicator"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "allow-get-status",
    "allow-recording-action"
  ]
}
